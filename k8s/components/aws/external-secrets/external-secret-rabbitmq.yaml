# AWS Secrets Manager 의 gamehouse/ENV/rabbitmq 시크릿(JSON) 을 15분마다 동기화해
# k8s Secret `rabbitmq-credentials` 을(를) 채운다. 키 이름은 Deployment 의 secretKeyRef 와 맞춰야 한다.
# 경로의 `ENV` 세그먼트는 각 오버레이의 `replacements` 가 dev/prod 로 치환한다
# (오버레이의 gamehouse-env ConfigMap 이 소스). 치환이 안 되면 SecretSyncedError 로 드러난다.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: rabbitmq-credentials
  namespace: gamehouse
spec:
  refreshInterval: 15m
  secretStoreRef:
    name: gamehouse-aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: rabbitmq-credentials
    creationPolicy: Owner
  data:
      - secretKey: RABBITMQ_USERNAME
        remoteRef:
          key: gamehouse/ENV/rabbitmq
          property: RABBITMQ_USERNAME
      - secretKey: RABBITMQ_PASSWORD
        remoteRef:
          key: gamehouse/ENV/rabbitmq
          property: RABBITMQ_PASSWORD
