# local: CORS 를 kind 프론트 오리진(http://gamehouse.local)으로. DB_HOST 는 secret-db-*.yaml 로 옮겼다.
apiVersion: v1
kind: ConfigMap
metadata:
  name: user-config
  namespace: gamehouse
data:
  CORS_ALLOWED_ORIGINS: "http://gamehouse.local"
  ENV_NAME: "local"
