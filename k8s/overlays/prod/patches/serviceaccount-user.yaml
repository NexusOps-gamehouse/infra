# ⚠️ CHANGE_ME: 실제 AWS 계정 ID/역할 이름으로 교체.
# 프로필 이미지 S3 버킷 읽기/쓰기 권한만 가진 IAM 역할을 IRSA 로 연결한다.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: user
  namespace: gamehouse
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::CHANGE_ME_ACCOUNT_ID:role/gamehouse-user-role
