# ⚠️ CHANGE_ME: 실제 AWS 계정 ID. 역할은 gamehouse/prod/* 만 읽도록 IAM 정책을 좁힌다.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets
  namespace: gamehouse
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::CHANGE_ME_ACCOUNT_ID:role/gamehouse-external-secrets-role
