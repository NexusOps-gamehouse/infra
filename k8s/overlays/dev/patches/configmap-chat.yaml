# dev: DB_HOST 를 in-cluster postgres 로, CORS 를 dev 프론트 오리진으로.
apiVersion: v1
kind: ConfigMap
metadata:
  name: chat-config
  namespace: gamehouse
data:
  DB_HOST: "postgres.gamehouse.svc.cluster.local"
  CORS_ALLOWED_ORIGINS: "http://gamehouse.local"
  ENV_NAME: "dev"
