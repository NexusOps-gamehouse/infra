# ⚠️ CHANGE_ME: dev AWS 계정 ID. 역할은 gamehouse/dev/* 만 읽도록 IAM 정책을 좁힌다.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets
  namespace: gamehouse
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::CHANGE_ME_ACCOUNT_ID:role/gamehouse-dev-external-secrets-role
