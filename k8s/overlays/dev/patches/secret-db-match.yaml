# ⚠️ dev 전용 더미값. kind 로컬 클러스터 밖으로 나가지 않으므로 커밋해도 안전하다.
# 운영 값은 여기 없다 — overlays/prod/external-secrets.yaml 참고.
apiVersion: v1
kind: Secret
metadata:
  name: match-db-credentials
  namespace: gamehouse
type: Opaque
stringData:
  DB_USERNAME: "duo"
  DB_PASSWORD: "dev-local-only-password"
