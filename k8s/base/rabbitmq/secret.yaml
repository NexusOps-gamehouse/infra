# ⚠️ placeholder. docker-compose.yml 의 RABBITMQ_USERNAME/PASSWORD 와 같은 역할.
# dev: overlays/dev 에서 더미값으로 patch. prod: External Secrets Operator 로 교체.
apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-credentials
  namespace: gamehouse
type: Opaque
stringData:
  RABBITMQ_USERNAME: "CHANGE_ME"
  RABBITMQ_PASSWORD: "CHANGE_ME"
