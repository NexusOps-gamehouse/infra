# ⚠️ placeholder. docker-compose.yml 의 RABBITMQ_USERNAME/PASSWORD 와 같은 역할.
# local: overlays/local 에서 kind 전용 더미값으로 patch.
# dev/prod: components/aws 가 이 Secret 을 지우고 External Secrets Operator 로 교체.
apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-credentials
  namespace: gamehouse
type: Opaque
stringData:
  RABBITMQ_USERNAME: "CHANGE_ME"
  RABBITMQ_PASSWORD: "CHANGE_ME"
