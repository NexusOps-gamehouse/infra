# 이 파일은 GameHouse MSA 설계 문서(디렉터리 구조 섹션 10)를 기준으로 생성되었다.
# 실제 값(이미지 태그, 리소스 상한, 시크릿)은 overlay 에서 환경별로 덮어쓴다.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: post
  namespace: gamehouse
  labels:
    app.kubernetes.io/name: post
  # prod overlay 에서 IRSA 를 붙인다 — 게시글 이미지 S3 버킷 접근용.
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::<ACCOUNT_ID>:role/gamehouse-post-role
automountServiceAccountToken: false
