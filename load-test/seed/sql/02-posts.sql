-- ===========================================================================
-- 게시글 시딩 — :posts 건. 작성자는 전부 seedauthor 다.
--
-- INSERT 본문은 _post-rows.sql 한 곳에만 있다. 회차 D 증분(d-growth-*.sql)도
-- 같은 파일을 부르므로, 값을 고치면 양쪽에 동시에 반영된다.
--
-- title 의 '[SEED]' 접두사가 계약이다.
--   · generate.sh 는 이 접두사로 post-ids.json 을 뽑는다
--   · cleanup/steady-state.sh 는 이 접두사가 붙은 글을 절대 지우지 않는다
--     (지우는 것은 시나리오 5 가 만든 '[LT]' 글이다)
-- 접두사를 바꾸면 세 곳이 동시에 깨진다.
-- ===========================================================================

\set target :posts
\ir _post-rows.sql
